# Background Music

把背景音乐文件放到这个目录即可（建议：`mp3` / `ogg` / `wav` / `m4a`）。

- 开发时：添加/删除音乐后，可能需要重启 `npm run dev` 才会刷新播放列表
- 打包时：`npm run build` 会自动把该目录下的音乐打进产物并在页面里按文件名顺序循环播放

注意：大多数浏览器会禁止“有声音的自动播放”。页面会尝试自动播放；如果被拦截，首次点击页面任意位置后会自动开始播放（或点“点一下开音乐”）。
